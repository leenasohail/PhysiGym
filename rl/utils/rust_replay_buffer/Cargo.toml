[package]
name = "rust_replay_buffer"
version = "0.1.0"
edition = "2025"

[lib]
name = "rust_replay_buffer"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
numpy = "0.20"
ndarray = { version = "0.15", features = ["serde"] }
rayon = "1.8"
